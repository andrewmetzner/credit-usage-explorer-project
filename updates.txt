================================================================
UPDATES LOG
================================================================

----------------------------------------------------------------
2026-06-25 (GUI polish + dark mode toggle)
----------------------------------------------------------------

- Dark mode: added a light/dark theme toggle (sun/moon) in the navbar,
  persisted in localStorage with a no-flash init script in <head>. Driven by
  CSS custom properties: expanded :root with semantic vars (--surface-2,
  --text, --text-soft, --faint, --hover, --accent-soft, --shadow) and a
  [data-theme="dark"] override block. Converted remaining hardcoded hex in
  app.css + inline template styles to vars, and added dark overrides for the
  Bootstrap pieces we use (form-control/select, dropdown-menu, badge.bg-light,
  btn-outline-secondary, btn-close, progress, table striping, modal, hr).
- Table/readability polish (reference: OpenAI admin dashboard): airier
  leaderboard rows, surface-2 sticky headers, tabular-nums on numeric cells +
  KPI/stat/metric values, and reference-style initials avatars on the Users
  cards (.avatar / .user-cell).
- Charts left unchanged (per request). All pages verified in light; theme
  variables verified present; diagnostics green.
- Note: app's own components + Bootstrap overrides are variable-driven, so new
  UI should use the CSS vars (var(--ink)/--text/--surface/--border/...) rather
  than hardcoded hex, to stay theme-correct.

----------------------------------------------------------------
2026-06-24 (Rename outliers page -> alerts)
----------------------------------------------------------------

- Renamed the rules page (formerly "outliers") to "alerts" for consistency:
    dashboard/outliers_routes.py  -> dashboard/alerts_routes.py
    register_outliers_routes()    -> register_alerts_routes()
    outliers_page()  /outliers    -> alerts_page()  /alerts
    /outliers/rules/*             -> /alerts/rules/*
    templates/outliers.html       -> templates/alerts.html
  Updated nav, notifications link, and the blueprint wiring. /outliers now 404s.
- KEPT the genuine outlier engine named "outliers" (it actually finds outliers):
  app/shared/outliers.py, compute_outliers, OUTLIER_VIEWS, the outlier_table
  macro, the advanced search and export_outliers. Renaming those would be wrong.
- Verified: no dangling refs, /alerts renders + nav active, all pages green.

----------------------------------------------------------------
2026-06-24 (Alerts page + outlier Excel export)
----------------------------------------------------------------

- Outliers page repurposed into an "Alerts" rules hub: it now focuses on
  creating/managing alert rules, with a prominent link to the advanced outlier
  search. Removed the inline metric-driven outlier table (the advanced search
  on the Users page now shows the records). Nav label renamed Outliers -> Alerts.
- Alert click-throughs (notification bell + each rule's "triggering"/View link)
  now deep-link to the advanced outlier search
  (/user-cards?mode=advanced&metric=...&credit_threshold=...&lookback_days=...)
  instead of the old outliers view.
- Excel export: the advanced search has an "Export to Excel" button that
  downloads the current outlier result as an .xlsx (openpyxl), labeled columns
  per view, for pasting into emails/discussions. New route
  analytics.export_outliers.
- Slimmed outliers_page route (no longer computes the view); dropped the now
  unused compute_outliers import there.
- Verified: Alerts page, deep-links resolve to advanced search, xlsx export
  (200 rows x 5 cols / per-record 6 cols), all pages + diagnostics green.

----------------------------------------------------------------
2026-06-24 (TODO: combined user + outlier search)
----------------------------------------------------------------

- Users page now has a Basic / Advanced mode toggle. Basic = the existing
  user-card grid + name/email search. Advanced = adds threshold / metric /
  lookback / type / model controls and lists the matching outlier records.
  The name/email search carries across both modes.
- Structure: moved the outlier engine (OUTLIER_VIEWS, compute_outliers, window
  helpers) out of dashboard/service.py into app/shared/outliers.py, so both the
  Outliers page (dashboard) and the Users advanced search (analytics) depend on
  shared logic instead of analytics reaching into dashboard.
- DRY: extracted the generic outlier/record table into a reusable Jinja macro
  (templates/macros/outlier_table.html), now used by both the Outliers page and
  the Users advanced mode. Also fixed a latent curly-quote bug on the user-card
  date-range element.
- Verified: outliers page intact, basic cards + advanced records both render,
  thresholds flag correctly, all pages + diagnostics green.

----------------------------------------------------------------
2026-06-24 (TODO: tiers chart + ML snapshots)
----------------------------------------------------------------

- Tiers: removed the chart-breakdown dropdown (the filters above already
  drive the chart). Chart now always shows the by-usage-type weekly breakdown;
  the route stops computing the unused model/I-O breakdowns.
- ML snapshots: every saved snapshot now also stores a linear-trend (ML)
  projection alongside the deterministic + Monte Carlo data. New ml_* columns
  in forecast_history.csv (slope, r_squared, p10/p50/p90 end balance) and an
  "ml" series (p10/p50/p90 + slope + r_squared) in each snapshot's JSON.
  Generated automatically (ML is cheap, so no skip flag needed in the weekly
  batch). Added a "Linear trend (ML)" overlay toggle on the forecast burndown
  chart, mirroring the Monte Carlo bands toggle.
- Refactor: extracted ForecastingService.build_forecast_context(), now shared
  by the model-data route, _run_mc, and the new _run_ml (removed the inline
  context-building duplicated in the route).

----------------------------------------------------------------
2026-06-24 (Typed AlertRule + shared DictMixin)
----------------------------------------------------------------

- New app/shared/alert_rules.py: an AlertRule dataclass replacing the loose
  {"metric":...,"threshold":...} dicts. Validation/coercion lives in one place
  (AlertRule.from_dict — clamps metric to a known value, coerces numbers,
  auto-generates ids); to_dict() serializes cleanly for JSON storage.
- Extracted the Mapping mixin from forecast/service.py into shared/typed.py
  (DictMixin) and reused it for AlertRule and the ContractStatus/Forecast
  objects, so all three share one implementation. Rules stay drop-in
  compatible with the template (r.name) and evaluate_rules (rule.get(...)).
- config_service load/save now parse/serialize through AlertRule; add/delete/
  toggle routes use typed access (toggle flips r.enabled, since a Mapping is
  read-only for item assignment).
- Verified: from_dict validation, JSON round-trip (saves plain dicts, loads
  AlertRule), evaluate_rules, all pages + diagnostics, forecast CSV unchanged.

----------------------------------------------------------------
2026-06-24 (Split dashboard/routes.py by concern)
----------------------------------------------------------------

- dashboard/routes.py was 433 lines mixing six concerns. Split into focused
  modules that each register onto the same `main` blueprint:
    routes.py (154)            core pages (index/summary/records) + wiring
    setup_routes.py (52)       first-run setup wizard
    diagnostics_routes.py (22) /debug + /debug.json
    outliers_routes.py (113)   outliers, notifications, alert-rule CRUD
    upload_routes.py (146)     multi-sheet upload + merge
- Endpoint names unchanged (main.setup_page, main.outliers_page,
  main.upload_data, ...), so every url_for("main.*") and the setup-guard
  exempt set stay valid. All 15 endpoints present; all pages + diagnostics
  verified green.

----------------------------------------------------------------
2026-06-24 (Typed forecast objects: ContractStatus / Forecast)
----------------------------------------------------------------

- get_contract_status() and get_forecast() now return typed dataclasses
  (ContractStatus, Forecast) instead of bare dicts, giving attribute access
  + IDE autocomplete + a single declared schema for every field.
- They subclass collections.abc.Mapping, so 100% backward compatible: every
  existing cs["x"], cs.get(...), {**cs, **fc}, .items(), pd.DataFrame([cs])
  and Jinja cs.x keeps working unchanged. Verified the snapshot CSVs serialize
  with byte-identical columns (22 + 19) and all pages + diagnostics stay green.

----------------------------------------------------------------
2026-06-24 (OOP refactor: Leaderboards)
----------------------------------------------------------------

- Moved the Tiers-page leaderboard aggregations (~80 lines of inline pandas
  for users / by-type / models / usage-types / biggest-single / daily /
  weekly / monthly / yearly) out of the route into a cohesive Leaderboards
  class in app/analytics/service.py. The route now reads as nine named
  method calls. Behaviour preserved (verified parity + all pages render).

----------------------------------------------------------------
2026-06-24 (OOP refactor: Services container)
----------------------------------------------------------------

- New app/shared/services.py: a Services container that bundles the shared
  singletons (store, pipeline, config_svc) and a build_forecasting_service()
  factory. All four blueprint factories now take a single `services` arg
  (was three args in inconsistent orders) and unpack what they need at the
  top — the rest of each route is unchanged.
- Centralized the ForecastingService construction that was copy-pasted in
  ~5 places (summary, upload auto-snapshot, alerts, diagnostics) into
  Services.build_forecasting_service(); future call sites just ask the
  container.
- compute_alerts(services) and Diagnostics(services) now take the container
  too. Removed the imports that were only type hints. (The diagnostics tool
  caught a stale-signature call during the refactor — working as intended.)
- All pages + diagnostics verified green.

----------------------------------------------------------------
2026-06-24 (CSS condensed to one app.css)
----------------------------------------------------------------

- Merged bnl.css + forecast.css into a single static/css/app.css and
  condensed: 819 lines across two files -> 562 in one. Introduced :root
  CSS variables (--surface/--bg/--border/--border-soft/--muted/--ink/
  --accent/--radius), a shared card-surface group and a shared uppercase
  label group (instead of repeating the same declarations ~12x), and
  organized into clear sections. Updated template references; removed the
  old files. All pages verified rendering.

----------------------------------------------------------------
2026-06-24 (CSS consolidation + OOP diagnostics)
----------------------------------------------------------------

CSS CONSOLIDATION
- Added a shared utility/component layer to bnl.css (cursor-pointer,
  toggle-label, scroll-box, w-rank, color-swatch, acc-item/acc-btn,
  text-2xs/xs/sm2, page-1100/1280) and migrated 30 repeated inline styles
  across templates to those classes. No <style> blocks remain anywhere.

BUILT-IN DIAGNOSTICS (object-oriented, extensible)
- New app/shared/diagnostics.py: a Check base class + DiagnosticResult,
  with Environment / Configuration / Data / Pipeline / Forecast / Alerts
  checks, and a Diagnostics runner that times each and rolls up an overall
  ok/warn/error status. Add a check by subclassing Check and appending to
  Diagnostics.CHECKS.
- New /debug (HTML dashboard) and /debug.json (machine-readable) routes,
  exempt from the setup guard so they work even when config is broken.
  Added a "Diagnostics" link in Settings.

----------------------------------------------------------------
2026-06-24 (tiers: switchable weekly chart)
----------------------------------------------------------------

- The Tiers weekly chart now has a breakdown dropdown in its toolbar:
  By usage type / By model / By input-output. Switching re-renders the
  chart instantly client-side (all three breakdowns are precomputed and
  embedded as a JSON island). Still reflects the page filters.

----------------------------------------------------------------
2026-06-24 (fix: upload with missing config dir)
----------------------------------------------------------------

- Bug: after deleting the config/ folder (e.g. while testing the wizard),
  uploading data failed with "Error saving merged data: ... config/
  data_path.txt (No such file or directory)" because the cache pointer
  couldn't be written into the missing folder.
- Fix: create_app now ensures CONFIG_DIR, PROCESSED_DIR, HISTORICAL_DIR,
  UPLOADS_DIR and the data dir exist on startup; the upload route also
  mkdir's the cache parent before writing. Verified by reproducing the
  missing-config-dir case — upload now succeeds and recreates the folder.

----------------------------------------------------------------
2026-06-23 (CSS condense + snapshot graph + setup wizard)
----------------------------------------------------------------

CSS CONDENSED
- Moved forecast.html's inline <style> block into static/css/forecast.css
  (loaded only on the forecast page). No template has a <style> block now.

SNAPSHOT COMPARISON GRAPH (smaller + optional)
- The comparison bar chart is off by default (the cards already show the
  data) and toggled by a "Chart" checkbox; height capped at 170px.

SETUP WIZARD (first-run)
- Config service is now robust to a missing contract_config.yaml (returns a
  default skeleton, never crashes); new contract_exists / is_contract_configured.
- New /setup wizard: Step 1 configure contract (dates, purchased credits,
  price), Step 2 upload data. Shows per-step done badges. Skippable.
- A before_request guard redirects all pages to /setup on a fresh install
  (no contract config) until you configure or skip; static + setup + upload
  endpoints are exempt. A "Setup wizard" link was added to Settings.

----------------------------------------------------------------
2026-06-23 (alert rule scoping)
----------------------------------------------------------------

- Alert rules can now be scoped by Usage type (all / codex / chat / ...)
  and Model. evaluate_rules applies the scope before counting; the alert
  detail states it ("...on codex") and the click-through deep-links the
  outlier view pre-filtered to that type/model. Rule rows show the scope
  as badges. New rule fields: usage_type, model (default = all).

----------------------------------------------------------------
2026-06-23 (outliers declutter + trigger-driven views)
----------------------------------------------------------------

- Alert Rules panel is now collapsed by default behind a "Manage rules (N)"
  toggle (shows an "N triggering" badge). The Notifications "Set up alert
  triggers" link opens it expanded.
- Metric-aware outlier views: compute_outliers replaces compute_outlier_users
  and serves three views via a "View" selector — Users over the window,
  User-days over the limit, and Single prompts over the limit. The table
  renders generically from a column spec returned by the service.
- Triggers drive views: clicking a rule alert (bell / notifications page /
  the rule's "triggering" status) opens the outliers page in the exact
  matching view, pre-filtered to that metric + threshold + window.

----------------------------------------------------------------
2026-06-23 (custom alert rules + date-range outliers)
----------------------------------------------------------------

CONFIGURABLE ALERT RULES (mutable outliers page)
- New "Alert Rules" section on the Outliers page: add/enable/disable/delete
  your own triggers. Three metrics: single prompt (per record), user total
  in one day, and user total over a window. Each shows a live triggering/
  clear status. Stored in config/alert_rules.json (AppConfig load/save).
- alerts.py now evaluates these rules (evaluate_rules) instead of the old
  hardcoded "heavy users" check; matches feed the notification bell.

OUTLIER FINDER: DATE-TO-DATE
- The finder now also accepts an explicit Start/End date range (overrides
  the rolling lookback). The window is shown in the header, the results
  column, and the active-filter badge.

NOTIFICATIONS: DAY-TO-DAY WINDOW
- Rule-based alerts now state the exact date window they cover, e.g.
  "4 users over 1,000 credits (2026-06-14 -> 2026-06-21)".

----------------------------------------------------------------
2026-06-23 (outlier perf + notifications page + chart fix)
----------------------------------------------------------------

OUTLIER FINDER OPTIMIZED
- compute_outlier_users now parses the date column once (was twice) and
  computes the per-user "top model" only for the small flagged set. Added
  a count_only fast path used by the alerts processor (runs on every page).
  Measured: count_only ~4.9ms vs full ~13.9ms on the live dataset; results
  identical.

NOTIFICATIONS PAGE + DROPDOWN CAP
- New /notifications page lists all current alerts full-width (read/unread
  + deep-links, same alerts.js state).
- The bell dropdown now shows the first 4 alerts with a "View all
  notifications" footer (shows the count when > 4). A hidden all-alert-ids
  holder keeps the unread badge + read-state correct even when the dropdown
  is capped.

USER SUMMARY CHART FIX
- The "Credits by type / week" stacked bar rendered unbounded-tall because
  the shared renderer uses maintainAspectRatio:false; wrapped its canvas in
  a fixed-height (190px) container so it's correctly sized/smaller.

----------------------------------------------------------------
2026-06-23 (notifications: read/unread + click-through)
----------------------------------------------------------------

- Each alert now has a stable id and optional deep-link args. Alerts carry
  read/unread state client-side (localStorage, new static/js/alerts.js):
  the bell badge shows the UNREAD count, read items are dimmed, and there's
  a "Mark all read" action. Clicking an alert marks it read. Resolved
  conditions are pruned so a recurring condition re-notifies.
- Click-through deep-links: the "heavy users" alert opens
  /outliers?credit_threshold=...&lookback_days=7 (pre-filtered to those
  users); exhaustion/overburning open the Forecast page.
- Addresses TODO "notifications read/unread" and "clicking a notification
  opens outliers / the relevant page".

----------------------------------------------------------------
2026-06-23 (outliers page + JS/template structure)
----------------------------------------------------------------

DEDICATED OUTLIERS PAGE
- Moved the Outlier Finder off the Summary page onto its own /outliers
  page (new nav item). Keeps the type/model filters; the "heavy users"
  alert now links here. Summary is leaner as a result.

HTML SIZE / "MORE JS" — data-island pattern
- Introduced the JSON data-island pattern: the template emits one
  <script type="application/json"> with server data, and all behavior
  lives in a cacheable static .js file (zero template interpolation).
- Summary: ~80 lines of inline JS -> static/js/summary.js (290 -> 220).
- Forecast: the ~1,100-line inline script -> static/js/forecast.js.
  forecast.html went 1,838 -> 756 lines (-59%). Functions stay at global
  scope (inline onclick still resolve); chart code guards on canvas
  existence so it self-skips on the no-data page. Verified: island is
  valid JSON, all D.* refs resolve, delimiters balanced, models 200.
- chart-card / chart-toolbar styles already live in global bnl.css; the
  reusable chart_card() Jinja macro + renderUsageTypeChart() keep new
  charts to ~2 template lines.

----------------------------------------------------------------
2026-06-23 (charts polish + outlier finder)
----------------------------------------------------------------

- Burndown chart: disabled vertical (y) pan/zoom — it now scrolls/zooms on
  the time axis only, so you can't drag the chart up into empty space.
- Users page (user summary): the middle "credits per week" bar is now the
  stacked "Credits by type / week" chart, matching Summary / Forecast /
  Tiers (one shared renderer).
- Outlier Finder: added a "Usage type" filter alongside Model, so you can
  find who is burning a lot on Codex, or on a specific Pro model, in the
  last N days. Shows an active-filter badge with a clear link.
- Confirmed the scikit LinearRegression model is registered alongside the
  Base (deterministic) and Monte Carlo models, with its own burndown
  overlay toggle on the Forecast chart.

IN-APP ALERTS (notifications)
- New app/shared/alerts.py -> compute_alerts(): evaluated on every page load
  via a context processor and rendered as a navbar bell with a severity
  badge + dropdown list. Fully guarded so it can't break page rendering.
- Alert types: stale data (>10 days old), heavy users this week (over a
  threshold in the last 7 days), forecast exhaustion risk, and overburning
  pace. Each links to the relevant page.

----------------------------------------------------------------
2026-06-23 (charts + ML)
----------------------------------------------------------------

CREDITS BY USAGE TYPE / WEEK (new chart)
- New shared builder app/shared/chart_data.py -> usage_type_weekly():
  weekly credits split by usage type (top-N + "Other"), order-ranked.
- Rendered as a stacked bar with a Stacked/Grouped toggle on THREE pages:
  Summary, Forecast, and Tiers (on Tiers it respects the page filters).

REUSABLE CHART INFRASTRUCTURE (less repetition, more structure)
- New Jinja macro app/templates/macros/charts.html -> chart_card(): one
  place for the card + toolbar (export PNG, fullscreen, optional zoom,
  optional bar-mode toggle). Used by all the new charts.
- chart-card / chart-toolbar styles moved into global bnl.css (removed the
  duplicated block from forecast.html).
- charts.js gained renderUsageTypeChart() + setUsageTypeStacked/Mode() so
  any page renders the chart with one call. Charts remain readable via the
  existing fullscreen modal + PNG export.

SCIKIT-LEARN LINEAR REGRESSION FORECAST MODEL
- Added scikit-learn to requirements; new LinearRegressionModel in
  app/forecast/prediction.py (registered alongside Deterministic + Monte
  Carlo). Fits weekly burn vs. week index, projects the trend forward, and
  derives P10/P90 bands from residual spread.
- ForecastContext gained an optional chronological weekly_series; the
  model-data endpoint now supplies it.
- New "Linear Trend (ML)" overlay toggle on the burndown chart (shows slope
  + R2), with state restored on reload and refreshed on granularity change.

----------------------------------------------------------------
2026-06-23
----------------------------------------------------------------

SNAPSHOT HISTORY TABLE (no more horizontal scroll)
- Rebuilt the snapshot table with a fixed 8-column layout that always
  fits the page width (no horizontal scrollbar). Columns consolidated:
  select+color swatch, Snapshot (date + editable label stacked), Status
  (pacing + forecast pills stacked), Weekly Burn, Remaining, End Balance
  (with exhaustion date as a sub-note), MC Risk, and actions.
- Added a scoped .snap-table style for a cleaner, more compact look.

MONTE CARLO IN SNAPSHOT OVERLAYS
- Added a "Monte Carlo bands" toggle in the quick-select bar beneath the
  burndown chart. When on, each selected snapshot's P10-P90 confidence
  bands are overlaid (for snapshots that carry MC data).
- "Generate weekly" is now a dropdown with two choices: Fast (no Monte
  Carlo) and Include Monte Carlo (slower). MC mode regenerates existing
  weekly snapshots so they gain bands, using a capped run count (<=2000)
  to stay responsive across many weeks.

----------------------------------------------------------------
2026-06-22 (later)
----------------------------------------------------------------

MULTI-SHEET UPLOAD
- The Summary page uploader now accepts multiple files at once. All
  selected sheets are merged into the current data in one action; order
  does not matter (merge is commutative).
- Added a "Replace current data instead of merging" checkbox for when a
  fresh start is wanted rather than appending.
- Uploaded sheets are read in memory and merged; the combined result is
  written as a single canonical current_data.csv (stale current_data.*
  siblings are cleaned up).

UPLOAD LOG (Settings)
- Every data-sheet upload is now recorded (new public method
  record_upload() on IngestionPipeline).
- The Settings "Upload Log" table now shows, per upload: when, type
  (data sheet / historical / weekly), file, rows Added, total rows, and
  credits. A "replace" badge marks replace-mode uploads.

FORECAST SNAPSHOTS UI (decluttered)
- Added a quick-select dropdown directly beneath the burndown chart to
  overlay any past snapshots, with removable pills showing the current
  selection. This replaces the wide, horizontally scrolling table as the
  primary way to compare snapshots.
- The full snapshot table is now tucked into a collapsible
  "Manage & details" panel (collapsed by default), so the page no longer
  scrolls sideways or feels cluttered.
- Comparison cards still appear automatically when 2+ snapshots are
  selected (via the dropdown or the table).

TESTING
- testfiles/test_data_merge.py - merge unit/scenario tests (unchanged).
- testfiles/verify_upload_route.py - new integration test driving the
  multi-file /upload-data route through the Flask test client; backs up
  and restores live data so nothing is disturbed.
- Verified: all templates compile, app factory imports, and the
  multi-upload route + logging pass end-to-end.

----------------------------------------------------------------
2026-06-22
----------------------------------------------------------------

DATA APPENDING / MERGING (foolproof, order-independent)
- Added new shared module: app/shared/data_merge.py
    * normalize_date_partition() - rewrites date_partition to canonical
      ISO (YYYY-MM-DD). Each file is parsed while still single-format so
      mixed formats (e.g. M/D/YYYY vs YYYY-MM-DD) can no longer corrupt
      comparison or storage.
    * merge_usage_data(existing, new) - single entry point for merging an
      uploaded file into the current data. Safe when there is no existing
      data (fresh load).
    * dedupe_usage() - keeps one row per natural record key
      (date_partition, account_id, user id, usage_type). On a value
      conflict the larger usage_credits is kept. Because max is
      commutative, the merged result is identical regardless of upload
      order. Falls back to exact-row dedup if a safe key cannot be built.

- Refactored app/dashboard/routes.py (upload-data handler):
    * Now reads the file and delegates to merge_usage_data().
    * Removed the previous inline append/dedup logic.
    * Upload now reports how many genuinely new records were added vs a
      full-overlap no-op, and surfaces errors as a flash message instead
      of failing.

BEHAVIOR GUARANTEES
- Overlapping files: each record counted once, never double-counted.
- Conflicting values for the same day/user/usage_type: larger credits
  wins, deterministically.
- Different date formats across files: normalized before any comparison.
- Any upload order: identical final result.
- Bad/unexpected files: handled gracefully (flash message, no crash).
- Filenames are ignored for date logic; only the in-file date_partition
  values are used (export filenames did not reflect actual data ranges).

NOTE ON CONFLICT RULE
- Current rule is "largest credits wins" (assumes a fuller export
  supersedes a partial one). Switching to "most recently uploaded wins"
  is a one-line change in dedupe_usage(), but would reintroduce
  order-dependence, so it was intentionally not used.

EARLIER SAME-DAY WORK
- Settings page: added "Export Data" section to download the current
  data file; tier tabs no longer show a horizontal scrollbar (wrap
  instead).
- Forecast page: added "Generate weekly" button to create one snapshot
  per operational week automatically (derives weekly data from the daily
  store when no pipeline weekly data exists; skips Monte Carlo for speed
  and skips weeks already snapshotted). Snapshot rename changed from
  double-click to an explicit pencil icon.

TESTING / HOUSEKEEPING
- Consolidated all scratch test scripts into testfiles/:
    * test_data_merge.py - mock order-independence, conflict resolution,
      mixed date formats, and a real-file round-trip through DataStore.
    * diagnose_data.py - inspects a batch of files for date ranges,
      record-key uniqueness, and cross-file value conflicts.
- Removed the temporary scripts from the project root; root now contains
  only config.py and run.py.
- All test-file paths resolve relatively from the repo root (no absolute
  or user-specific paths hard-coded).
- Verified: full test suite passes and the Flask app factory imports
  cleanly.

================================================================
